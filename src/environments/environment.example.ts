// Vorlage: kopiere diese Datei nach `environment.ts` und trage deine
// Supabase-Credentials ein. `environment.ts` ist gitignored und wird
// nicht committet.
//
// Project Settings → API → "Project URL" und "anon public" Key.
export const environment = {
  production: false,
  supabaseUrl: '',
  supabaseAnonKey: ''
};
